import { IceAmountPipe } from './ice-amount.pipe';

describe('IceAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new IceAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
